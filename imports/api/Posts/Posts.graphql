# body
# createdAt
# author
# [commentId]

type Post {
    _id: String!
    body: String!
    category: String!
}

type Mutation {
    createPost(body: String!, category: String!): Post
}

type Query {
    posts: [Post]
}
